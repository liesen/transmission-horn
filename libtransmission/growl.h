#include "transmission.h"

void tr_torrentSetGrowlCompletionCallback(tr_torrent * tor,
                                          const char * hostname,
                                          unsigned short port,
                                          const char * password);
